services:
  bookshelf:
    image: ghcr.io/quichef/marsubooks:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/books.db
      - PORT=8080
